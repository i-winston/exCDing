<div class="nested-fields well well-compact">
  <div class="field">
    <%= f.label :disk_name, "ディスク名：" %>
    <%= f.text_field :disk_name, placeholder: "新規登録するディスク名を入力" %>
    <%= link_to_remove_association "ディスク削除", f, {class: "btn btn-danger btn-xs", role: "button"} %>
  </div>

  <div id="songs">
    <%= f.fields_for :songs do |s| %>
      <%= render 'song_edit_fields', f: s %>
    <% end %>
  </div>

  <div id="links">
    <%= link_to_add_association "曲を追加",f, :songs, {class: "btn btn-primary btn-xs", role: "button"} %>
  </div>
</div>
